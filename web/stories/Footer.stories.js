import React from 'react'

import { storiesOf } from '@storybook/react'
import { action } from '@storybook/addon-actions'
import Footer from '../components/Footer'

storiesOf('Footer', module).add('Default', () => <Footer></Footer>)
